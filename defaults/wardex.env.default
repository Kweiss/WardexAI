# Conservative default mode for both MCP and hook integrations.
WARDEX_MODE=guardian

# Isolated signer socket location.
WARDEX_SIGNER_SOCKET=/tmp/wardex-signer.sock

# MCP server transport defaults.
WARDEX_TRANSPORT=stdio
WARDEX_PORT=3100
WARDEX_HTTP_HOST=127.0.0.1
# Optional: enable Bearer auth for /mcp and / endpoints when using HTTP transport.
# WARDEX_HTTP_AUTH_TOKEN=replace-with-long-random-token
